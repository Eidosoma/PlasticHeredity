# Lay summary

One or more repairs survived both the fresh selection screen and the full causal checks. They are candidates, not confirmed results.

This round first measured how the daughter changes the inherited texture recipe, then tested simple rescaling and a universal error-corrector. The error-corrector never sees which ancestral form is desired; it only learns a common copying grammar from already exposed lineages.

The final 96 pairs remain untouched until a person reviews these selection results and explicitly authorizes confirmation.
