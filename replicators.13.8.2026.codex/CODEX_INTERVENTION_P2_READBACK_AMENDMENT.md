# P2 physical-rule readback-recovery amendment

Status: preregistered after P2 stopped and before any P2 effect size, arm rate, candidate comparison, interval, p-value, or verdict was inspected.

P2 completed all 51,200 primary and 51,200 exact-replay F12 futures across 400 restored states. It then encountered the same non-scientific dictionary mismatch diagnosed in P1: generation adds the derived `pilot_eligibility` Boolean, while the original readback path compared dictionaries before deriving that replay-dependent field. Atomic output cleanup left no sealed P2 result, and P3 was not launched.

This amendment changes no simulator, endpoint, matrix, state, physical catalytic-support rule, random arm, selected intervention, seed, future, replay, model, bootstrap, randomization, multiplicity correction, margin, gate, or claim boundary. Original registration `f61e0340dcd8c9ae6b606c8133ca3d8fb1de2e13fe863719aa67b649e8b74531` and all of its scientific sources remain unchanged.

Before loading checkpoint outcomes, the recovery implementation must checksum-seal:

- this amendment;
- its phase-specific recovery source and tests;
- the shared recovery writer;
- the already sealed P1 derived-field readback helper;
- the original registration and failure log hashes; and
- aggregate hashes of all 400 generation and 400 replay state checkpoints.

Recovery may reconstruct the deterministic natural cohort and load completed checkpoints only. It must fail if a checkpoint is missing or changed and may generate or regenerate zero intervention futures. Readback pilot eligibility is derived exactly as:

```text
readback pilot eligibility =
    readback pilot eligibility without replay
    AND exact replay
```

The full primary metrics and matrix effects must then match their saved-file recomputation exactly. Checkpoint aggregate hashes must remain unchanged through sealing. The recovered P2 result—positive, mixed, or negative—must be reported without modification, followed by the mandatory stop before P3.

