# Codex Chapter 5 PX10: multiscale causal-information adjudication

Status: prospective registration source. No PX10 scientific catalytic matrix,
restored state, intervention future, or scientific information reading may be
generated until the source, tests, validation, calibration, frozen models,
protocol, and seed registry are sealed.

## Purpose and immutable predecessors

PX10 is a Codex-only closing adjudication of three questions left by PX9:

1. Is the pairing-corrected PX8/PX9 whole-minus-parts statistic a reliable but
   behaviorally redundant temporal echo of post-break renewal?
2. Do preregistered PhiID atom groups show a calibrated, multiscale response to
   renewal-directed molecular interventions?
3. How many held-out bits about renewal are causally conveyed by the identity
   of a uniformly assigned edit-dose arm?

PX10 does not import, execute, or compare against Fable code, data, models,
states, seeds, selected edits, or results. Published definitions may motivate
synthetic fixtures, but every implementation and scientific artifact is
independently produced by Codex.

All earlier results remain immutable. In particular, PX10 cannot overwrite the
null public nine-atom result, the PX9 `finite_sample_or_marginal_explanation`
classification, or any causal plastic-heredity result.

## Pre-scientific calibration

The existing Codex estimators are frozen. Calibration cannot select a new
formula, support, sign, partition, atom weight, redundancy rule, or
regularizer.

Calibration contains:

- an independent exact-distribution reference for two-bit discrete systems;
- independent, copy, cross-transfer, downward-XOR, parity-preserving-random,
  and common-driver fixtures;
- analytic linear-Gaussian transition systems;
- marginal-preserving temporal derangements; and
- 99-dimensional finite-sample Gaussian/CLR surrogates at supports 128, 256,
  512, 1,024, and 2,048.

Required numerical identities use absolute tolerance `1e-10`. The exact
discrete reference uses the same pointwise-redundancy then expectation order
as the frozen local estimator; it must recover each complete registered atom
signature in at least 95% of 1,024-sample replicates and 80% of 128-sample
replicates. Unlisted atoms are exactly zero:

| Fixture | Registered nonzero atoms |
|---|---|
| copy | `r->r=+1`, `u0->u1=-1`, `u1->u0=-1`, `s->u0=+1`, `s->u1=+1`, `u0->s=+1`, `u1->s=+1`, `s->s=-1` |
| cross-transfer | `r->r=+1`, `u0->u0=-1`, `u1->u1=-1`, `s->u0=+1`, `s->u1=+1`, `u0->s=+1`, `u1->s=+1`, `s->s=-1` |
| downward XOR | `s->r=+1`, `s->u1=-1`, `s->s=+1` |
| parity preservation | `s->s=+1` |
| common driver | `r->r=+1` |

The frozen
continuous estimator must recover analytic Gaussian score signs in at least
90% of 128-sample replicates and have at most 10% median relative error at
1,024 samples. The eight-dimensional analytic fixture uses within-coordinate
transition weight 0.45, cross-half weight +0.15/-0.1125, and isotropic noise
variance 0.8. The 99-dimensional Gaussian/CLR surrogate is descriptive and
audits finite-support behavior rather than defining a new pass threshold.
Independent and deranged null means must lie within +/-0.01
bits.

Calibration failure makes the multiscale atomic fingerprint mechanistically
ineligible. It does not stop the frozen temporal-echo confirmation or the
randomized intervention-channel analysis. Ineligible atom results remain
descriptive and cannot pass a scientific gate.

## Shared fresh cohort

- 48 entirely fresh catalytic matrices shared across candidates 02 and 03.
- Natural landmarks 20, 35, 50, 65, and 80.
- From each untreated landmark, follow at most 60 fissions to the first strict
  inheritance break (`H <= 0.9`).
- Save and restore the exact selected daughter immediately after that break.
- Require at least 40 distinct eligible matrices per candidate.
- Never replace a failed, extinct, or adverse matrix.
- Use the frozen candidate-separated CR5 renewal students without refit or
  recalibration.
- Exhaustively score all legal mass-preserving one-molecule substitutions.
- Select `Q00`, `Q20`, `Q40`, `Q60`, `Q80`, and `Q100` by fixed empirical
  predicted-probability quantiles with deterministic ties.
- Include one uniformly sampled `RANDOM` legal edit and `NOOP`.
- Launch 256 F8 futures per arm and state.
- Half A is branches 0--127; half B is branches 128--255.
- Complete a second deterministic replay of every future.

The arm is absent from the future seed. Random edit selection uses a separate
stream. Once edited states diverge, arms share common random streams, not
identical realized futures.

## PX10-B1: frozen temporal-echo confirmation

The primary score remains exactly:

    T_temporal = T_beta(real parent-daughter pairs)
                 - mean of eight depth-preserving daughter derangements

where `T_beta = I(X;X') - I(A;A') - I(B;B')` uses the fixed unedited-beta
Fiedler partition and 128 source branches per half.

The plastic-heredity manipulation must pass in both candidates and halves:

- positive `Q100-Q00` renewal probability;
- positive 95% whole-matrix bootstrap lower bound;
- Holm-adjusted whole-matrix randomization `p < 0.05`;
- `RANDOM-NOOP` equivalence within +/-0.025 probability; and
- positive within-state six-dose Spearman association.

Temporal authenticity requires, in every registered cell:

- positive `Q100-Q00` response of `T_temporal`;
- positive A/B reliability in both candidates;
- positive source-to-opposite-half renewal forecasting; and
- positive within-state dose concordance with opposite-half renewal.

Every directional gate uses a positive point estimate, positive 95% lower
bound, and Holm-adjusted `p < 0.05`.

Incremental branch-level log-loss gain over the frozen ordinary-process
baseline is reported in bits. It is:

- nonredundant when positive with a positive 95% lower bound and adjusted
  `p < 0.05` in all four cells;
- redundant when every 90% interval lies within +/-0.0005 bits per branch;
- unresolved otherwise.

Registered classifications are:

- `reliable_nonredundant_temporal_gauge`;
- `reliable_redundant_temporal_echo`;
- `temporal_echo_reliability_unresolved`; or
- `temporal_echo_not_confirmed`.

The exact public revised nine-atom score remains a non-winning negative
control.

## PX10-B2: multiscale atomic fingerprint

Use explicit within-branch generational pairs at lags 1, 2, 3, and 4. Lag 8 is
registered secondary. Branches and extinction boundaries are never crossed.

The atomic fingerprint uses only its confirmatory `Q00` and `Q100` arms. From
each registered 128-branch half it takes the first fixed nested 64 branches,
then pools explicit pairs over all eligible landmarks within the matrix. The
full 256 branches remain in the hereditary endpoint, intervention-channel,
and exploratory macro/micro analyses. This fixed atom support was chosen
before sealing to keep the complete replay below 30 process CPU-hours while
retaining approximately 1,600--2,560 transitions per primary lag in a fully
eligible matrix.

Use the fixed unedited-beta Fiedler partition across arms. Score all 16 PhiID
atoms and the following frozen groups:

- downward causation: `s->u0 + s->u1`;
- synergy persistence: `s->s`;
- emergence: `s->u0 + s->u1 + s->s`.

For each lag and group, subtract the mean of eight marginal-preserving,
depth-specific future derangements. Compare the beta partition with eight
deterministic size-matched random partitions. To control compute without
altering the question, atom scores pool explicit pairs across eligible
landmarks within each catalytic matrix before scoring. Inference remains at
the whole-matrix level.

The primary multiscale value is the unweighted mean over lags 1--4; no best lag
may be selected. Registered `Q100-Q00` directions are:

- downward causation: positive;
- synergy persistence: negative;
- emergence: positive.

`atomic_temporal_fingerprint` requires calibration eligibility and every
direction-aligned effect to pass in both candidates and halves after Holm
correction. `atomic_beta_specificity` additionally requires the beta-partition
effect to exceed the median size-matched random-partition effect in all cells.

## PX10-C: intervention-channel information

The six quantile arms form an equally weighted intervention variable `U`.
Three-consecutive-fission renewal within F8 is binary outcome `R`.

For A-to-B and B-to-A separately:

1. estimate `P(R | state, U)` on the source half with Jeffreys smoothing;
2. estimate state-only `P(R | state)` after pooling the six arms;
3. score both on the untouched target half; and
4. report the target-half log-loss improvement in bits per branch.

The confirmatory channel passes only when all four candidate/direction cells
have positive gain, a positive 95% matrix-bootstrap lower bound, and
Holm-adjusted randomization `p < 0.05`.

Sixteen deterministic, within-state and branch-balanced source-label
derangements and the `RANDOM` versus `NOOP` two-action channel must each be
equivalent to zero within +/-0.0005 bits per branch. Each label derangement
preserves every arm count but varies across branch indices, destroying a
stable action-to-outcome mapping rather than globally renaming the arms.

Uniform-input plug-in mutual information and binary-output channel capacity
are registered secondary descriptions. A positive result applies only to this
registered local action panel and endpoint.

## Exploratory macro/micro prototype

This analysis cannot rescue a failed confirmatory phase.

Use only the archived CR5 development feature matrix to fit candidate-specific
standardization and PCA. No outcomes enter the transform. Signs relative to
development medians on the first four PCs define up to 16 micro bins. The first
two bits define four nested macro bins.

Map edited starting states and selected daughters into the frozen bins. Build
one-fission kernels with Dirichlet-1/2 smoothing and form macro kernels by
uniform causal marginalization over observed micro bins. Report effective
information, normalized effectiveness, determinism, degeneracy, and
macro-minus-micro differences by candidate and arm. Repeat at lag 3 as a
secondary description.

Because these are feature-defined empirical grains rather than an exhaustive
intervention over every molecular microstate, report the result as an
`exploratory macro/micro causal-emergence prototype`, never as established
causal emergence.

## Inference, execution, and stopping

- Catalytic matrix is the inference and resampling unit.
- Candidates and branch directions are never pooled to rescue disagreement.
- Use 4,096 whole-matrix bootstrap draws and 4,096 paired sign randomizations.
- Apply Holm correction within each named family.
- All code, tests, calibration, protocol, model hashes, and seed domains are
  sealed before scientific matrix generation.
- Use at most eight workers and 30 process CPU-hours.
- Scientific execution is detached and checkpointed per matrix.
- Dense tables, arrays, checkpoints, and logs remain ignored by Git and may be
  stored on the larger local volume.
- If the CPU budget is reached, preserve checkpoints and report an incomplete
  campaign; do not change matrices, branches, arms, lags, replay, or inference.
- No automatic continuation beyond 48 matrices is authorized.

## Claim boundaries

PX10 may support a Codex-specific temporal information echo, multiscale atom
fingerprint, or causal intervention channel. It cannot make an information
statistic the cause of plastic heredity, rescue the exact public Phi-r by a new
definition, establish consciousness, agency, life, biological memory, a
universal origin-of-life mechanism, real prebiotic chemistry, Platonic space,
or the Ruliad.
