# Preregistered CA motif-lineage Stage 3

Date frozen: 2026-08-22. This protocol is frozen before any Stage-3 daughter
trajectory is generated. The user explicitly authorized Stage 3 after reviewing
the completed Stage-2 generalization result.

## Decisive question and frozen mechanism

Stage 3 asks whether the robust motif channel closes a self-renewing lineage
loop. A parent-written carrier must guide a visibly reset daughter; that
daughter must reconstruct the carrier from its own recovered texture; and the
cycle must persist for 16 generations. The reader remains the exact Stage-1/2
winner: a 512-entry 3x3 motif-energy carrier, 32 founder write sweeps, read
strength 0.25, and 32 daughter read sweeps. No Stage-3 outcome may tune it.

The clean-room exclusion continues: no Wagner or Fable implementation source
is read, imported, hashed, or executed.

## Fresh cohort

Every pair used in Stage 1, the two Stage-2 development-only pairs, and all 96
Stage-2 reference pairs are excluded. Remaining Rule-31649 pairs are ordered by
the new `plastic-ca-motif-lineage-stage3-v1` namespace. The first two are
permanently reserved for smoke testing before scientific output is inspected;
the next 64 form the untouched reference cohort. Each reference pair receives
64 paired futures per parental history.

## Generation lifecycle

Every founder writes its carrier from 32 post-update sweeps. Every descendant
generation starts from its pair's bitwise-identical native neutral board and
runs for 64 sweeps under process noise 0.002. The inherited carrier is read only
during sweeps 1--32. It is never read during sweeps 33--64.

The daughter writer counts all 3x3 motifs during sweeps 33--64, converts them to
the same smoothed log-frequency difference from the frozen Stage-1 reference,
clips to [-4,4], and uses that newly written table as the next generation's
carrier. Thus the reader cannot directly copy itself into the writer: renewal
must be mediated by the daughter's visible spacetime texture after reading has
stopped. A branch empty at generation end dies, remains in all denominators,
and cannot reproduce.

An actively written carrier replaces the inherited table at the boundary. If
writing is disabled, the inherited table retains exactly 50% of its amplitude
per generation. This registered decay makes indefinite persistence of a static
founder template distinguishable from active renewal.

Phenotypes and carrier separation are recorded after generations 1, 2, 4, 8,
and 16. The primary phenotype observer remains trailing-eight-sweep accumulated
live 2x2 texture; terminal 2x2, connected components, and the independent
texture descriptor remain diagnostics.

## Causal intervention ladder

Every pair receives:

- intact read/write lineages;
- carrier zeroed at every boundary;
- carrier addresses shuffled at every boundary;
- reading disabled;
- founder writing disabled;
- rewriting disabled after the founder, with 50% decay;
- carrier ablation after generation 2, entering generation 3;
- the same ablation followed by a contemporaneous intact-sister rescue entering generation 4;
- the same ablation followed by an opposite-history intact-sister rescue entering generation 4;
- opposite founder-carrier transfer entering generation 1;
- one-percent carrier-sign corruption at every boundary.

Intact sisters are simulated first and never receive information from an
intervention branch. Rescue is matched by pair, future, and generation.
Semantic process and reader randomness is paired across histories and
conditions.

## Registered inference

The pair is the independent unit. Dead and unresolved futures remain in
denominators. Reference intervals use 10,000 pair-cluster bootstrap draws with
familywise alpha 0.025 for the causal family.

`RENEWED_CA_PLASTIC_HEREDITY` requires:

- intact crossover at least 0.15 at generation 8 and 0.10 at generation 16,
  both with positive lower bounds;
- both directions positive, at least half of pairs positive, survival at least
  0.90, and terminal-observer crossover at least 0.10;
- intact advantages at least 0.10 with positive bounds over zero, shuffle,
  read-disabled, and founder-write-disabled lineages at generation 8;
- at least 70% loss without rewriting by generation 8;
- at least 70% loss after generation-2 ablation at generation 4;
- restoration to at least 70% of intact by same-history rescue, plus rescue
  advantage at least 0.10 over ablation;
- opposite-history rescue crossover at most -0.10 at generation 4;
- opposite-founder crossover at most -0.10 at generation 8; and
- crossover at least 0.10 under repeated one-percent carrier corruption.

An intact effect that persists without rewriting is `STATIC_HIDDEN_TEMPLATE`.
An effect present at generation 8 but absent at generation 16 is
`TRANSIENT_LINEAGE_MEMORY`. An incomplete run cannot pass.

## Detached execution and next gate

The reference job uses 20 workers, stops submitting checkpoints at 7 hours 30
minutes, and has a hard eight-hour wall deadline. Checkpoints are atomic and
bound to the complete protocol, code, cohort, prior-stage, configuration, and
reference hashes. `STATUS.json` is the polling interface; `--resume` accepts
only the identical design.

The run freezes `RESULTS.json`, `REPORT.md`, `LAY_SUMMARY.md`,
`STAGE_DECISION.json`, and `QUEUE.json`. Stage 4 compression cannot launch
automatically. A positive verdict is a synthetic CA plastic-heredity mechanism,
not evidence of metabolism, agency, biological life, or memory outside the
automaton's total physical state.
