# Hypothesis-source quarantine

The `NewIdeas/preprints/ingressing-minds-v-ruliad-paper-ideas/` materials are
used only to generate questions for this follow-up.  In particular, they
suggest separating arrival from residence, testing counterfactual futures
rather than snapshot similarity alone, using occupancy-matched controls, and
including an exact relabeling control.

No result, number, classification, data row, code path, or claimed conclusion
from `NewIdeas` enters donor selection, simulation, scoring, inference, or the
current preprint's evidence chain.  The runner records hashes of the named
hypothesis documents in a separate manifest and never imports their code or
reads their outcome artifacts.

This campaign is reviewer-prompted and frozen before its new futures.  It is
not preregistered before the earlier strict-eight or lineage-identity results.

