# Strict-B transplant, rehoming, and rare-form stress test

This isolated follow-up distinguishes lineage/state memory, a shared
rule-conditioned destination, and a rare transient coherent excursion.  It
uses the already verified 50-rule strict-capable cohort and B banks only to
freeze donors; every tested stochastic future uses a fresh seed namespace.

The primary experiment is a four-arm label-permutation factorial: native
state/native rule, state-only permutation, rule-only permutation, and exact
joint permutation.  A natural other-lineage B is descriptive.  A separately
labelled case-study challenges the two operational forms previously detected
for matrices 11, 54, and 63.

All `NewIdeas` material is hypothesis-generating only, as specified in
`HYPOTHESIS_BOUNDARY.md`.  Nothing in this package edits the manuscript.

## Commands

From `replicators.13.8.2026.codex`:

```bash
.venv/bin/python -m pytest -q \
  reviewer_lineage_identity_response/followup_transplant_arrival_residence/test_transplant.py

.venv/bin/python -m \
  reviewer_lineage_identity_response.followup_transplant_arrival_residence.run_analysis \
  prepare

reviewer_lineage_identity_response/followup_transplant_arrival_residence/run_detached_pipeline.sh 16
```

The launcher returns immediately and records progress, throughput, and ETA in
`artifacts/STATUS.json`.  All simulation checkpoints are resumable only under
the exact matching protocol digest.

